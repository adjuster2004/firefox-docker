# firefox-docker - браузер в контейнере

Браузер работает на хостовой машине

Для изменения содержимого файла /etc/hosts используется закладка с адресом http://localhost:5000/

Если нет возможности изменить содержимое файла /etc/hosts, но очень нужно, можно воспользоваться контейнером с браузером.

## ✨ Преимущества
-  **все запускается несколькими командами**
-  **не требуются права sudo для внесения DNS записей**
-  **не требуются права на DNS сервере**
-  **работает без vnc**
-  **темная тема**


## 🛠️ Последовательность
- **Склонировать репозиторий**
```bash
git clone https://github.com/adjuster2004/firefox-docker/
cd firefox-docker
```

- **Настроить права на исполняемые файлы**
```bash
chmod +x run.sh stop.sh fix-permissions.sh
```

- **Собираем образ**
```bash
docker build -t firefox-hosts-editor .
```

- **Запускаем контейнер**
```bash
docker run -d \
  --name firefox-hosts-editor \
  -e DISPLAY=$DISPLAY \
  -v /tmp/.X11-unix:/tmp/.X11-unix \
  -p 5000:5000 \
  -v "$(pwd)/data/hosts/system:/etc/hosts:rw" \
  --shm-size=2g \
  --privileged \
  firefox-hosts-editor
```

- **Либо воспользоваться настроенным файлом**
```bash
./run.sh
```

- **Содержимое файла /etc/hosts можно менять через веб**
```
http://localhost:5000/
```

<img width="861" height="482" alt="image" src="https://github.com/user-attachments/assets/6524b683-0eb2-42ca-b109-590cff513536" />


 
## 📄 Лицензия
Этот проект распространяется под лицензией **MIT**.

Copyright (c) 2025 Sergey S @adjuster2004

Подробности в файле [LICENSE](LICENSE).
